services:
  api:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "8080:8080"
    command: ["node", "src/server.js"]

  worker:
    build: .
    restart: unless-stopped
    env_file: .env
    command: ["node", "src/worker.js"]
